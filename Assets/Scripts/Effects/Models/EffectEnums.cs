namespace Effects.Data {
    public enum EffectType {
        UNSET,
        POISON,
        STUN,
        BURN
    }

    public enum EffectTiming {
        UNSET,
        BEFORE,
        AFTER
    }
}
