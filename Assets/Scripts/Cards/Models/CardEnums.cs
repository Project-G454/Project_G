namespace Cards {
    public enum CardTypes {
        UNSET,
        ATTACK,
        MAGIC,
        MOVE,
        DEFENCE
    }

    public enum CardRarity {
        UNSET,
        COMMON,
        UNCOMMON,
        RARE,
        EPIC,
        LEGENDARY
    }
}
