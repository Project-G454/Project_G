namespace Entities {
    public enum EntityTypes {
        UNSET,
        PLAYER,
        ENEMY
    }

    public enum EntityClasses {
        UNSET,
        WARRIOR,
        RANGER,
        ROGUE,
        WIZARD,
        Boss,
        Minion
    }

    public enum MoveState {
        Idle,
        Moving
    }
}
